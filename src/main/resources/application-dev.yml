spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://builzer-database:3306/builzer
    username: root
    password: 11111111

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error